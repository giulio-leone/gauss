/**
 * Standalone A2A delegation sub-path export.
 * Import as: `gauss/a2a`
 * No Node.js or AI SDK dependencies — safe for browser/extension contexts.
 */
export { A2ADelegationManager } from '../plugins/a2a-delegation.js';
export type {
  AgentCapability,
  DelegationResult,
} from '../plugins/a2a-delegation.js';
